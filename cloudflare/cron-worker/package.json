{
  "name": "tradebot-cron-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker that triggers the TradeBot automation cycle every 5 minutes via Cron Trigger",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.60.0"
  }
}
